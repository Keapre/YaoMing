# Pusher deploy report

6 August 2026, 09:18

## What is being deployed

| | |
|---|---|
| APK | 65.8 MB |
| native libraries | 9.7 MB in the APK, 23.2 MB once extracted |
| dex handed to dexopt | 34.7 MB across 25 files |
| library packing | compressed, so the install extracts 23.2 MB |

The install is not just a copy. The package manager writes the APK into
`/data/app`, verifies its signature, extracts the native libraries if they are
compressed, and runs dexopt over every dex file. That is why it takes as long
as it does, and it is why the settings below split into transfer and install.

## Measured

Each is a real deploy to the robot. The baseline is what Android Studio does:
one streamed install of the whole APK, no delta, no skipping.

| configuration | time | vs Android Studio | what it did |
|---|---|---|---|
| Android Studio equivalent | 44.9s | about the same | one streamed session install of the whole APK, no delta |
| pusher, staged install | 41.7s | 1.1x faster | push to a temporary file, then install from it |
| pusher, streamed install | 41.3s | 1.1x faster | stream the APK into the install session |
| pusher, delta transfer | 40.6s | 1.1x faster | send only changed chunks, then install |
| pusher, delta + streamed | 39.9s | 1.1x faster | changed chunks, streamed into the session |
| pusher, nothing changed | 243ms | 184.6x faster | the robot already holds this exact build |

## What each setting does

`pusher settings` -> Deploy speed

| setting | now | effect here |
|---|---|---|
| Send only changed parts | on | 40.6s vs 41.7s staged (about the same) |
| Skip install when unchanged | on | 243ms when nothing changed |
| Stream the install | on | 41.3s vs 41.7s staged (about the same) |
| Store native libraries | off | would remove 23.2 MB of extraction at install; APK grows by roughly 13.5 MB |
| Install only changed splits | off | the project builds one APK, so there is nothing to split |

Two of these are not free. Storing the libraries makes the APK bigger, which
costs transfer time, so it is a win on USB or 5 GHz and a question on 2.4 GHz.
Delta costs a little work on the hub to rebuild the APK, which is why it can
lose over USB where the transfer was never the problem.

Not swept here, because changing them needs a rebuild:

- **One ABI** (`pusher slim`): the APK carries 9.7 MB of native libraries. A stock
  project packages two architectures and the hub runs one.
- **Gradle threads**: build time only, nothing to do with the deploy.

## Hot reload, if it existed

Pusher does not hot reload. This is a measurement of what it would cost on
this hub, so the decision to build it rests on numbers from the hardware
rather than on somebody else's.

| | |
|---|---|
| dex used | 0 (the smallest in the APK, standing in for team code) |
| pushing it | 0s |
| dex2oat on the hub | not measured:  |
| floor for a reload | at least 0s |

What this does **not** prove: that the classes would load. The FTC SDK finds
OpModes by walking the base APK, then the split APKs, then whatever OnBotJava
and the external-libraries mechanism report. Anything loaded outside those
paths is invisible to the Driver Station no matter how fast it arrived.

## Against Sloth

**Pusher is not a Sloth replacement.** Sloth hot reloads: it sends only the
team's code and loads it into a running app. Pusher installs an APK faster.
Those are different problems, and below a few seconds they are not comparable.

Their published figures, quoted from their README rather than measured here:

| | claimed |
|---|---|
| Sloth, typical | under 1s |
| Sloth, worst case | about 2s |
| FastLoad | about 7s |
| a standard TeamCode upload | 40s+ |

Fastest real deploy measured here: **39.9s** (pusher, delta + streamed).

Read that as a floor, not a headline. Everything pusher does still ends in a
package manager install, and no amount of transfer cleverness removes dexopt.
Getting into Sloth's range means not installing at all.
