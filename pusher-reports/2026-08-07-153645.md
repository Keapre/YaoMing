# Pusher Extreme reload

7 August 2026, 15:36

204 classes, 43 of them registered with FtcDashboard, 727 KB sent.

| stage | best of 5 | spread |
|---|---|---|
| classpath (gradle) | 307ms | 216ms |
| compile (javac + d8) | 1.47s | 173ms |
| push and trigger | 1.09s | 112ms |
| **total** | **2.89s** | 484ms |

Timed from the start of the command to the robot being told to reload.
The robot picks it up on its next event loop tick, which is not separately
measurable from here.

Every run resolves the classpath, because a deploy does. That stage is the
obvious thing to cache, and nothing has been cached yet.
