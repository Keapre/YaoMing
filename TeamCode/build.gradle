//
// build.gradle in TeamCode
//
// Most of the definitions for building your module reside in a common, shared
// file 'build.common.gradle'. Being factored in this way makes it easier to
// integrate updates to the FTC into your code. If you really need to customize
// the build definitions, you can place those customizations in this file, but
// please think carefully as to whether such customizations are really necessary
// before doing so.


// Custom definitions may go here

// Include common definitions from above.
apply from: '../build.common.gradle'
apply from: '../build.dependencies.gradle'

android {
    namespace = 'org.firstinspires.ftc.teamcode'

    compileSdk 35
    defaultConfig {
        minSdk 24
    }

    packagingOptions {
        jniLibs.useLegacyPackaging false
    }
}

dependencies {
    implementation project(':FtcRobotController')
    implementation "com.bylazar:fullpanels"
    implementation 'org.ftclib.ftclib:core:2.1.1'

    // blob path follower. `pusher settings` manages this line.
    implementation files('libs/blob-dev-v1.5.0.aar')
}

// pusher extreme: begin - team code is reloaded, not packaged
//
// Remove this block, or run the menu entry that added it, to go back to
// packaging team code in the APK.
//
// The Pinpoint driver used to be carved out of this exclusion, because the robot config needed an
// @I2cDeviceType class in the APK and ours lived in TeamCode. blob ships its own driver inside the
// AAR now, and an AAR is a dependency rather than team source, so it is packaged regardless. No
// carve-out needed.
android {
    sourceSets {
        main {
            java {
                exclude { details ->
                    def path = details.path
                    !details.directory && path.startsWith('org/firstinspires/ftc/teamcode/')
                }
            }
        }
    }
}
// pusher extreme: end
